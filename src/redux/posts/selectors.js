export const selectAllPosts = (state) => state.posts.postsList;

export const selectComments = (state) => state.posts.comments;

export const selectUserPosts = (state) => state.posts.userPostsList;


